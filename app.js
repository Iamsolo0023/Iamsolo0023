const canvas=document.getElementById('neural-bg');if(canvas){const ctx=canvas.getContext('2d');let W=0,H=0,dpr=1,scrollY=0,t=0;const mouse={x:0,y:0},target={x:0,y:0};const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let pts=[];function resize(){W=innerWidth;H=innerHeight;dpr=Math.min(devicePixelRatio||1,2);canvas.width=W*dpr;canvas.height=H*dpr;canvas.style.width=W+'px';canvas.style.height=H+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const n=W<700?95:W<1100?150:230;pts=Array.from({length:n},(_,i)=>{const a=Math.random()*Math.PI*2,r=Math.pow(Math.random(),.55)*(Math.min(W,H)*.42);return{x:W*.70+Math.cos(a)*r,y:H*.48+Math.sin(a)*r*.72,z:.15+Math.random()*.95,px:0,py:0,s:.3+Math.random()*.8,p:i}})}function project(p){const cx=W*.69+(mouse.x-W*.5)*.035,cy=H*.47+(mouse.y-H*.5)*.025-scrollY*.018,depth=1.05+p.z*.65;return{x:cx+(p.x-W*.69)/depth,y:cy+(p.y-H*.47)/depth}}function frame(){t+=.006;ctx.clearRect(0,0,W,H);const cx=W*.69+(mouse.x-W*.5)*.035,cy=H*.47+(mouse.y-H*.5)*.025-scrollY*.018,R=Math.min(W,H)*.31;for(const p of pts){if(!reduce){p.x+=Math.cos(t+p.p)*.08*p.s;p.y+=Math.sin(t*1.13+p.p)*.05*p.s}const q=project(p);p.px=q.x;p.py=q.y}for(let i=0;i<pts.length;i++){const a=pts[i];for(let j=i+1;j<pts.length;j++){const b=pts[j],dx=a.px-b.px,dy=a.py-b.py,d=Math.hypot(dx,dy);if(d<95){ctx.beginPath();ctx.moveTo(a.px,a.py);ctx.lineTo(b.px,b.py);ctx.strokeStyle=`rgba(155,124,255,${.18*(1-d/95)})`;ctx.lineWidth=.55;ctx.stroke()}}}for(const p of pts){const q=project(p),size=(.7+p.z*1.7)*(W<700?.75:1);ctx.beginPath();ctx.arc(q.x,q.y,size,0,Math.PI*2);ctx.fillStyle=`rgba(190,165,255,${.32+p.z*.45})`;ctx.shadowBlur=9+p.z*8;ctx.shadowColor='rgba(155,124,255,.65)';ctx.fill();ctx.shadowBlur=0}ctx.save();ctx.translate(cx,cy);ctx.rotate(mouse.x*.08);const bands=15,cols=20;for(let b=0;b<bands;b++){const lat=-Math.PI/2+(b+.5)*Math.PI/bands,ry=Math.cos(lat),yy=Math.sin(lat)*R;ctx.beginPath();for(let k=0;k<=80;k++){const a=k/80*Math.PI*2+t*.18,x=Math.cos(a)*R*ry,y=yy*.72;k?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.strokeStyle=`rgba(155,124,255,${.10+ry*.11})`;ctx.lineWidth=.65;ctx.stroke()}for(let c=0;c<cols;c++){const a=c/cols*Math.PI+t*.12;ctx.beginPath();for(let k=0;k<=80;k++){const lat=-Math.PI/2+k/80*Math.PI,x=Math.cos(a)*Math.cos(lat)*R,y=Math.sin(lat)*R*.72;k?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.strokeStyle='rgba(85,230,224,.13)';ctx.lineWidth=.65;ctx.stroke()}for(let k=0;k<4;k++){ctx.beginPath();ctx.ellipse(0,0,R*(1+k*.08),R*(.16+k*.025),t+k,0,Math.PI*2);ctx.strokeStyle='rgba(85,230,224,.12)';ctx.stroke()}ctx.restore();for(let k=0;k<3;k++){ctx.save();ctx.translate(cx,cy);ctx.rotate(t*(.35-k*.09)+k);ctx.beginPath();ctx.ellipse(0,0,R*(1.22+k*.16),R*(.27+k*.04),0,0,Math.PI*2);ctx.strokeStyle=k===1?'rgba(85,230,224,.32)':'rgba(155,124,255,.27)';ctx.lineWidth=k===1?1.2:.8;ctx.stroke();ctx.restore()}if(!reduce)requestAnimationFrame(frame)}resize();frame();addEventListener('resize',resize,{passive:true});addEventListener('pointermove',e=>{target.x=e.clientX;target.y=e.clientY;mouse.x+=(target.x-mouse.x)*.06;mouse.y+=(target.y-mouse.y)*.06},{passive:true});addEventListener('scroll',()=>scrollY=window.scrollY,{passive:true})}const backTop=document.getElementById('backTop');if(backTop){const sync=()=>backTop.classList.toggle('visible',scrollY>320);addEventListener('scroll',sync,{passive:true});sync();backTop.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}))}const cursor=document.querySelector('.cursor');addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});document.querySelectorAll('a,button').forEach(el=>{el.addEventListener('mouseenter',()=>document.body.classList.add('hovering'));el.addEventListener('mouseleave',()=>document.body.classList.remove('hovering'))});document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.projects article').forEach(card=>card.style.display=f==='all'||card.dataset.tags.includes(f)?'flex':'none')}));document.querySelector('.hamb')?.addEventListener('click',()=>{const links=document.querySelector('.links');links.style.display=links.style.display==='flex'?'none':'flex';links.style.position='absolute';links.style.top='76px';links.style.left='0';links.style.right='0';links.style.padding='25px 6vw';links.style.background='#0a0a0e';links.style.flexDirection='column';links.style.borderBottom='1px solid #25242c'});const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('seen')}),{threshold:.12});document.querySelectorAll('.projects article,.timeline article,.skill').forEach(x=>{x.style.opacity='0';x.style.transform='translateY(18px)';x.style.transition='opacity .65s ease,transform .65s ease';obs.observe(x)});const style=document.createElement('style');style.textContent='.seen{opacity:1!important;transform:none!important}.cursor{position:fixed;width:10px;height:10px;border:1px solid #9b7cff;border-radius:50%;pointer-events:none;z-index:100;transform:translate(-50%,-50%);mix-blend-mode:difference}@media(max-width:850px){.cursor{display:none}}';document.head.appendChild(style);